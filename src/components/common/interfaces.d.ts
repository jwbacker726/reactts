interface IContactPageProps {
  key : string;
  vikingsFan: boolean;
  twinsFan: boolean;
  wildFan: boolean;
}

interface IContactPageState {
  name: string;
  address: string;
  phone: string;
  favoriteColor: string;
}
